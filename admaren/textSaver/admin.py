from typing import Text
from textSaver.models import Tag
from django.contrib import admin
from textSaver.models import Tag,Text

# Register your models here.
admin.site.register(Tag)
admin.site.register(Text)
